# Runtime deps for the Greyhawk/OSRIC campaign server.
# Tier 1 (engine + state) needs only the Python stdlib; these are for the web
# layer that lands in Tier 1 step 6.
fastapi
uvicorn[standard]
pydantic
websockets
mcp>=1.10.0   # Streamable HTTP transport (server/http_server.py) needs a recent SDK
# anthropic no longer required: Pass-2 validation runs through the model-agnostic
# OpenAI-compatible client (referee/llm.py). Install only for a native Claude path.
# anthropic>=0.40.0
